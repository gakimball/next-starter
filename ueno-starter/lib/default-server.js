/**
 * This server file is used if a project doesn't have a `server/index.js` file. This allows the
 * user to have the fancy default server without needing a boilerpalte file.
 */

const server = require('../server');

server();
